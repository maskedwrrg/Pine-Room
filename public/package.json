{
  "name": "websocket-chat",
  "version": "1.0.0",
  "description": "A simple WebSocket chat room",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^4.18.0",
    "ws": "^8.13.0"
  }
}
